<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link rel="preconnect" href="https://fonts.googleapis.com">
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
<link href="https://fonts.googleapis.com/css2?family=Caveat:wght@400..700&family=Whisper&display=swap" rel="stylesheet">
<link rel="preconnect" href="https://fonts.googleapis.com">
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
<link href="https://fonts.googleapis.com/css2?family=Caveat&family=Special+Elite&family=Whisper&display=swap" rel="stylesheet">
<link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous">
<link rel="stylesheet" href="css/style.css">
<title>Quotely!</title>
</head>

<body>
  <div>
    <header>
      <h1>
        <a href="/">QUOTELY</a>
      </h1>
      <nav>
        <ul>
            <li><a class="headline" href="/">Home</a></li>
            {{#if logged_in}}
                <li><a class="headline" href="/dashboard">Dashboard</a></li>
                <li><a class="headline" id="logout" href="/">Logout</a></li>
            {{else}}
                <li><a class="headline" href="/login">Sign In</a></li>
            {{/if}}
        </ul>
      </nav>
    </header>
    <main class="container container-fluid mt-5">
      <!-- Render the sub layout -->
      {{{ body }}} 
    </main>
    <footer>
      {{#if quoteOfTheDay}}
        <div class="quote-of-the-day">
          <h2>Quote of the Day</h2>
          <p>"{{quoteOfTheDay.quote}}" - {{quoteOfTheDay.author}}</p>
        </div>
      {{/if}}
    </footer>
  </div> 

  <!-- Render script for logged in users only -->
  {{#if logged_in}}
  <script src="/js/logout.js"></script>
  {{/if}}
</body>

</html>